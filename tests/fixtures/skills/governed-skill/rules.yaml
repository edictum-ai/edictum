apiVersion: edictum/v1
kind: Ruleset
metadata:
  name: file-manager-rules
  description: Contracts for the file manager skill
rules:
  - id: restrict-to-workspace
    type: sandbox
    tools:
      - "file_*"
    within:
      - /workspace
    outside:
      action: block
      message: "File operations restricted to /workspace"
