kind: Ruleset

metadata:
  name: test-bundle

defaults:
  mode: enforce

rules:
  - id: test
    type: pre
    tool: read_file
    when:
      args.path:
        contains: ".env"
    then:
      action: block
      message: "denied"
