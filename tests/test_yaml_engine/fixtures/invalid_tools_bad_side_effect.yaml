apiVersion: edictum/v1
kind: Ruleset

metadata:
  name: bad-tools

defaults:
  mode: enforce

tools:
  explode_tool:
    side_effect: explode

rules:
  - id: placeholder
    type: pre
    tool: "*"
    when:
      args.path:
        exists: true
    then:
      action: block
      message: "Denied."
