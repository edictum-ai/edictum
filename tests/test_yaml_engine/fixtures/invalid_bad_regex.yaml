apiVersion: edictum/v1
kind: Ruleset

metadata:
  name: bad-regex

defaults:
  mode: enforce

rules:
  - id: bad-regex
    type: pre
    tool: bash
    when:
      args.command:
        matches: '[invalid('
    then:
      action: block
      message: "This has an invalid regex."
