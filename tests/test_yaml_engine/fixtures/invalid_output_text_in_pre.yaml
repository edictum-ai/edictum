apiVersion: edictum/v1
kind: Ruleset

metadata:
  name: invalid-output-text-in-pre

defaults:
  mode: enforce

rules:
  - id: bad-pre-with-output
    type: pre
    tool: "*"
    when:
      output.text:
        contains: "secret"
    then:
      action: block
      message: "Should not be valid."
